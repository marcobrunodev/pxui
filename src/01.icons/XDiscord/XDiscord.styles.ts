import styled from 'styled-components'
import discord from '../../assets/icons/discord.png'

const XDiscord = styled.img.attrs({ src: discord, alt: 'Discord icon' }) <HTMLImageElement>``

const S = {
  XDiscord
}

export default S