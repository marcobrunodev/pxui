import styled from 'styled-components'
import loading from '../../assets/icons/flags/loading-flag.gif'

const XLoadingFlag = styled.img.attrs({ src: loading, alt: 'Loading flag' })``

const S = {
  XLoadingFlag
}

export default S