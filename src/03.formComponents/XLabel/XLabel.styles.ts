import styled from 'styled-components'
import { XText } from '../../02.components/XText/XText.styles'

export const XLabel = styled(XText)`
  font-size: 1.7rem;
`

const S = {
  XLabel
}

export default S