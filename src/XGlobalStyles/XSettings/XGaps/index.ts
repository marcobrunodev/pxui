import { createGlobalStyle } from 'styled-components'

const XGaps = createGlobalStyle`
  :root {
    --gap-smaller: 0.4rem;
    --gap-small: 0.8rem;
    --gap-medium: 1.6rem;
    --gap-big: 2.4rem;
    --gap-bigger: 3.2rem;
  }
`

export default XGaps
