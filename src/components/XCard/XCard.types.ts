export type XCardTypes = {
  titleContent: string,
  image: string,
  alt: string,
  description: string,
  borderAnimation: boolean,
  noMargin?: boolean,
  primary?: boolean
}

export default XCardTypes